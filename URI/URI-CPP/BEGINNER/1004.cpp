#include <iostream>

using namespace std;

int main() {
    int number1, number2, PROD; // Initialize Variable
    cin >> number1 >> number2; // Take input
    PROD = number1 * number2; // Product of number1 & number2
    cout << "PROD = " << PROD << endl; // output the PROD
    return 0;
}