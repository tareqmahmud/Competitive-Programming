#include <iostream>

using namespace std;

int main() {
    int A, B, C, D; // Initialize Variable
    cin >> A >> B >> C >> D; // Take input
    cout << "DIFERENCA = " << (A * B - C * D) << endl; // Find difference of two product and output that product
    return 0;
}